modLoader="javafml" #mandatory
loaderVersion="[29,)" #mandatory (28 is current forge version)
credits="Thanks for this example mod goes to Java" #optional
authors="bageldotjpg" #optional

[[mods]] #mandatory
modId="crumbsandcritters" #mandatory
version="0.0.2" #mandatory
displayName="crumbsandcritters" #mandatory
description='''
Learning how to mod.
'''

[[dependencies.crumbsandcritters]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[29,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"
[[dependencies.crumbsandcritters]]
    modId="minecraft"
    mandatory=true
    versionRange="[1.15]"
    ordering="NONE"
    side="BOTH"
